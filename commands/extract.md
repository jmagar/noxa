---
description: Extract structured data from a URL using an LLM (--extract-prompt or --extract-json)
argument-hint: --extract-prompt "<query>" <url> | --extract-json '<schema>' <url>
---

Output: !`noxa $ARGUMENTS`

Present the extracted data to the user.

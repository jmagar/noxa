---
description: Discover all URLs on a site via its sitemaps
argument-hint: <url> [flags passed directly to noxa]
---

Output: !`noxa --map $ARGUMENTS`

Present the discovered URLs to the user with a count and any notable path patterns.

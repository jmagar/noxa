---
description: Recursively crawl a site with noxa
argument-hint: <url> [flags passed directly to noxa]
---

Output: !`noxa --crawl $ARGUMENTS`

Report the result to the user, including where pages are being saved and how to check progress with `noxa --status <domain>`.

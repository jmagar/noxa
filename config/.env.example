# Copy this file to .env.
# config/config.example.json -> config.json for non-secret defaults.
# config/config.schema.json documents the JSON keys accepted in config.json.
# Secrets, URLs, and path overrides only — everything else goes in config.json.

# Cloud API key (required for --cloud / --research, optional for search)
NOXA_API_KEY=
# Self-hosted SearXNG base URL for local search.
# Search uses this and does not require NOXA_API_KEY when set.
# This may be a localhost/private operator endpoint.
# SEARXNG_URL=https://your-searxng-instance.example.com

# Single proxy URL (or use NOXA_PROXY_FILE for pool rotation)
NOXA_PROXY=

# Proxy pool file path for rotating proxies
NOXA_PROXY_FILE=

# Webhook URL for completion notifications
# Operator-supplied endpoints like this may be localhost/private.
NOXA_WEBHOOK_URL=

# LLM provider configuration and backend defaults
# NOXA_LLM_PROVIDER=gemini
# NOXA_LLM_MODEL=gemini-2.5-pro
# NOXA_LLM_BASE_URL= (Ollama or OpenAI-compatible endpoint)
# GEMINI_MODEL=gemini-2.5-pro
# OLLAMA_HOST=http://localhost:11434
# OLLAMA_MODEL=qwen3.5:9b
# OLLAMA_HEALTH_TIMEOUT_MS=2000
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=

# Optional Axon / Neo4j integration placeholders
# AXON_NEO4J_URL=
# AXON_NEO4J_USER=
# AXON_NEO4J_PASSWORD=

# Disable automatic content persistence (~/.noxa/content/).
# Set to any non-empty value to opt out of ContentStore writes for all runs.
# Per-run opt-out: noxa --no-store https://example.com
# NOXA_NO_STORE=1

# Optional: path to a non-default config file (default: ./config.json)
# NOXA_CONFIG=/path/to/my-config.json
